import { PropertyChangeRecord } from './interfaces';
export declare const enum DiffType {
    CUSTOM = 1,
    ALWAYS = 2,
    IGNORE = 3,
    REFERENCE = 4,
    SHALLOW = 5,
    AUTO = 6,
}
export default function diff(propertyName: string, diffDiffType: DiffType, previousProperty: any, newProperty: any, meta?: any): PropertyChangeRecord;
