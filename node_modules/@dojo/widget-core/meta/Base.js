(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "@dojo/core/global"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var global_1 = require("@dojo/core/global");
    var Base = (function () {
        function Base(properties) {
            this._invalidate = properties.invalidate;
            this._requiredNodes = properties.requiredNodes;
            this.nodes = properties.nodes;
        }
        Base.prototype.has = function (key) {
            this.requireNode(key);
            return this.nodes.has(key);
        };
        Base.prototype.invalidate = function () {
            global_1.default.cancelAnimationFrame(this._invalidating);
            this._invalidating = global_1.default.requestAnimationFrame(this._invalidate);
        };
        Base.prototype.requireNode = function (key) {
            this._requiredNodes.add(key);
            if (!this.nodes.has(key)) {
                this.invalidate();
            }
        };
        return Base;
    }());
    exports.Base = Base;
    exports.default = Base;
});
//# sourceMappingURL=Base.js.map